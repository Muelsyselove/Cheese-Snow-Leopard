# 自主知识库桌面应用 — 依赖清单

# 桌面 UI
PySide6>=6.6.0

# Web UI 内核（默认 UI 路线：内置 WebView2 窗口，非外部浏览器）
pywebview>=6.0

# PDF 渲染
PyMuPDF>=1.24.0

# VLM 方案A：PaddleOCR-VL（CPU 可运行）
paddlepaddle>=2.6.0
paddleocr>=2.7.0

# VLM 方案B：MinerU 框架（含 vlm/pipeline 两后端）
# mineru>=2.5.0

# VLM 方案C：MiniCPM-V 4.5（需 GPU）
# lmdeploy>=0.4.0

# Embedding 方案A：BGE-M3（三模态）
FlagEmbedding>=1.2.10

# Embedding 方案B：Qwen3-Embedding（纯 dense）
# transformers>=4.44.0
# torch>=2.3.0

# 向量库
qdrant-client>=1.10.0

# 元数据存储
psycopg2-binary>=2.9.9
SQLAlchemy>=2.0.0

# 对象存储
minio>=7.2.0

# Agent 编排
langgraph>=0.2.0
langchain-core>=0.3.0

# 文字 LLM API（OpenAI 兼容）
openai>=1.40.0

# 配置
PyYAML>=6.0.0

# 凭据安全存储
keyring>=25.0.0

# 重试
tenacity>=8.5.0

# ===== 开发依赖（不在运行时安装，开发时手动 pip install -r requirements-dev.txt）=====
# 见 requirements-dev.txt：
#   pytest>=8.0.0
#   pytest-cov>=5.0.0
#   pytest-qt>=4.4.0
#   testcontainers>=4.7.0
#   ruff>=0.6.0
#   mypy>=1.11.0
